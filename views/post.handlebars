{{> post-info post }}

{{#if loggedIn}}
<form class="comment-form">
  <div>
    <textarea cols="100" rows="10" name="comment-body"></textarea>
  </div>

  <button class ="btn" id="add-comment" type="submit">Add comment</button>
  
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}